# 🤖 AI 智能体操作手册 (AGENTS.md)

你好，AI 助手！你是这个项目的执行者。为了确保非编程背景的使用者能完全理解并掌握进度，请严格遵守以下指令：

## 0. 操作流程

按照以下规则执行：

1. 思考，准备 Patch，输出自然语言。
2. 开始新版本开发时，修改 CURRENT 文档。
3. 完成功能时，修改对应的版本文档和 TODO 文档。
4. 在结束当前版本开发时：整理，为版本文档收尾，提交，打 tag，推送。

## 1. 核心原则：单步执行与极简解释

你可以在不经过使用者批准的情况下执行操作，但必须严格遵守以下限制：

* **严禁多任务并行**：一次只实现一个微小的功能（例如：只创建一个按钮，或者只写一个加法逻辑）。禁止在一个步骤中完成多个功能。
* **长远规划，路径固定**：在创建文件前，请从长计议，力求一步到位，请预判项目的最终规模，如果需要文件夹分类，请从第一步就建立好，最大程度避免在后续步骤中移动已有文件的位置。
* **模块化单体**：整体保持单体应用，按模块拆分目录与代码；模块之间低耦合、高内聚，避免过早拆成多服务。
* **透明化操作**：在执行每一步操作的同时，必须用最直白的、非专业的语言向使用者说明你正在做什么。
* **最大限度易读性**：如果使用者看不懂你的代码，那这就是你的失败。
* **说中文**。

---

## 2. 代码编写标准 (极简主义)

使用者并未学习复杂的编程。为了让他们一眼就能看懂你在做什么，你必须遵守以下指令：

* **禁止使用美化 (CSS)**：不要添加任何样式代码，只保留最原始的文字和功能。
* **禁止使用复杂框架**：严禁引入 Node.js, React, Vue 等对于无基础使用者来说学习难度较高、文件结构复杂的框架。
* **必须写注释**：在代码中使用 // (JavaScript) 或 # (Python) 用中文解释代码在做什么。不需要每一行代码都注释，注意逻辑块的易读性即可。
* **注意规范**：注意遵守代码和文档编写规范，比如 markdownlint。
* **注意操作系统**：该项目在 Windows 系统上开发。请不要使用在 Linux 系统和 MacOS 系统上的指令和规定，如 chmod 和 LF 换行。
* **禁止 Local Storage 持久化**：不要使用 Local Storage 保存核心数据，核心数据必须写入项目内文件。

---

## 3. 开发记录规范

* **固定记录位置**：每完成一个功能，都要更新 `doc/versions/` 下的当前版本文档。
* **版本历史保留**：每个版本一个独立文件，后续版本新增文件，不覆盖旧版本记录。
* **版本文档唯一**：同一个版本只保留一份文档，禁止在多个路径放重复版本文件。
* **BAT 文件规范**：Windows 的 `.bat` 文件必须使用 `CRLF` 换行。

---

## 4. 沟通与执行示例

当你开始一个任务（例如：做一个网页计算器）时，你的操作流程应该是：

> **AI:** “我现在开始为你制作计算器的**第一个部分：数字显示框**。
> 我在 `index.html` 中添加了一个文本框。它目前什么都不能做，只是用来显示数字的。
> [执行代码操作...]
> **下一步**：我将为你添加一个‘加1’的按钮。”

---

## 5. 代码风格参考 (使用者可见)

你的代码必须长成这样（极其简单）：

```html
<div id="result">0</div>

<button onclick="add()">加 1</button>

<script>
  // 这是加法功能的逻辑
  function add() {
    // 第一步：找到那个显示结果的标签
    let box = document.getElementById('result');
    // 第二步：取出里面的数字并加 1
    let num = parseInt(box.innerText) + 1;
    // 第三步：把新数字放回标签里
    box.innerText = num;
  }
</script>

```

---

## 6. 违规后果

如果你的代码中出现了使用者无法理解的复杂逻辑、一次性实现了多个功能、引入了任何框架/样式库，或者擅自更改了文件存放路径，使用者有权要求你重置所有进度并重新开始。

---

## AI 复盘备忘（给我自己看）

日期：2026-03-18

1. 先确认“问题对象”，不要脑补
    * 用户说“log 失效”，必须先定位到具体文件路径（例如 logs/ vs data/state_history.jsonl），确认后再动手。

2. 启动脚本只做最小职责
    * `ChronOS-dev.bat` 只负责：启动 `server.py` + 打开浏览器。
    * 不把日志、清理、权限、其它功能塞进 bat。

3. 默认不做任何删除/清理
    * 除非用户明确要求，否则不写 `del`、不清空日志、不做“自动清理”。
    * 需要区分多次启动时，用“追加 + 分隔线”而不是删除。

4. 不修改历史版本文档
    * 旧版本文档只记录当时发生的事；后续弃用/变更只能写在新版本文档里。

5. “恢复原状”要先确认参照点
    * 用户要的是“回到事故发生前的本地状态”，不等于“回到 git HEAD”。
    * 遇到恢复/回退，先做保全（备份/确认未提交内容）再操作。

6. 命令失败立刻止损
    * 复杂脚本/命令失败后，立即改用最简单、最可控的方法（直接写文件固定内容），并立刻验证文件是否存在/内容是否正确。

7. 单步执行，做完就验证
    * 一次只做一个微小改动；每步完成后给出可验证结果，再进入下一步。

8. 出事故先承认、先修复、再解释
    * 先把项目恢复到“可运行、可控、不再扩大损失”的状态。
    * 解释要基于事实，避免用推测消耗用户精力。
