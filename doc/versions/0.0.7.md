## 需求

1. 通知功能。

## 已完成功能

1. 增加“系统通知（测试）”按钮。
2. 通知逻辑已解耦：放在 src/notify.js，主程序 src/main.js 只做调用接线。
3. （已弃用）不再维护 `ChronOS.vbs`。
4. 统一两种日志格式：HTTP 访问日志改为走 `log()`，与系统日志一样都带 `[YYYY-MM-DD HH:MM:SS]` 时间戳前缀。
5. 进一步统一日志结构：全部改为 `[时间] [级别] [来源] 内容`，并把爬虫输出按行转发为 `[CRAWLER]` 来源，避免双时间戳。
6. 开发模式支持“改完代码自动重启服务”：新增 `tools/dev_autoreload.py`，并让 `ChronOS-dev.bat` 默认启动这个自动重启器。
7. 修复开发模式日志偶发“换行异常”：自动重启器改为按行转发子进程输出，并加打印锁，避免多线程输出把一行拆开。
8. 增加重启分隔线：每次自动重启前后都会打印明显边界，清楚区分“重启开始/重启完成”。
9. 日志改为单文件：统一写入 `logs/server.log`（包含 reloader 与 server 输出），不再使用 `dev/auto` 命名。
