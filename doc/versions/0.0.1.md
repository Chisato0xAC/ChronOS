## 需求

1. DP、GP 的显示。
2. DP 的修改。
3. 数据库存储。
4. 搭建服务器，实现在局域网下使用手机连接。

## 已完成功能（明细）

- 从 `data/state.json` 读取 DP、GP 并显示到页面。
- 支持 DP `+1`、`-1` 按钮修改，且不小于 0。
- 支持输入表达式修改 DP（例如：`+10-3`、`10-3`、`-5`）。
- 提供 `POST /api/save-dp` 接口，把 DP 写回 `data/state.json`。
- `server.py` 支持页面 GET 访问与 API POST 保存。
